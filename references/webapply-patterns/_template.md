---
domain: example.zhiye.com        # 单公司写具体域名；系统级共性写通配，如 "*.zhiye.com"
system: 北森|Moka|大易|智联|自研|未知
aliases: [公司中文名, 常见别称]
updated: YYYY-MM-DD
verified_on: [在哪次填表中验证过，注明公司与日期]
---

## 系统识别特征

怎么从 DOM／URL 认出这套系统：组件类名前缀、URL 模式、登录页特征、页脚的系统供应商标识。
——这一节决定了下次遇到陌生公司能不能直接套用本文件。

## 登录与入口

网申入口的 URL 模式、登录方式、"投递/申请"按钮在哪、投完之后跳到哪。

## 表单分步结构

向导有几步、每步叫什么、顺序如何；哪一步有"保存草稿"；能不能中途退出再回来续填。

## 字段与控件对照表

| 字段 | 控件类型 | 定位方法（选择器或 textContent） | 填写方法 | 备注 |
|---|---|---|---|---|

## 提交与保存按钮辨识

终局按钮（提交/确认投递）的文案与选择器，以及它和"保存/下一步"在 DOM 上怎么区分。
**这一节直接关系到会不会误触不可逆操作，写准确。**

## 已知陷阱

什么操作会失败、为什么、绕过办法。每条标上发现日期。

## 变更记录

- YYYY-MM-DD 初建 / 更新了什么

---

> **写入规则**：本文件只记录页面结构、选择器和操作策略。
> 绝不写入任何个人数据（姓名、手机号、邮箱、证件信息）、开放题答案文本、或账号信息。
> 存盘前按 `webapply.md` 第 10 节的 grep 自检过一遍。
